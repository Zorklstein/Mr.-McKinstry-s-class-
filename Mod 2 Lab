#Ezekiel Mesman 
#files name: Mod 2 Lab
#app discription: simples program for getting a students name and grading information
#to determine if student is on deans list and honor roll

while True:
    lastName = input("What is the students last name(or ZZZ to quit): ")
    if lastName.upper() == 'ZZZ':
        print("Thank you for using this program.")
        break

    firstName = input("What is the students first name: ")
    gpa = float(input(f"what is {firstName.title()}'s GPA(entered as a decimal): "))
    
    if gpa >= 3.5:
        print(f"{firstName} {lastName} is on the Dean's List and Honor Roll.")
    elif gpa >= 3.25:
        print(f"{firstName} {lastName} is on the Honor Roll.")
    else:
        print(f"{firstName} {lastName} does not qualify for the Dean's List or Honor Roll.")
